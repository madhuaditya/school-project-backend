// const express = require('express');
// const { validateUser } = require('../middleware/auth');
// const { allow } = require('../middleware/role');
// const {
//   addMaintenance,
//   updateMaintenance,
//   getMaintenance,
// } = require('../controllers/maintenanceCtrl');

// const r = express.Router();

// r.post('/', validateUser, allow('manager'), addMaintenance);
// r.put('/:id', validateUser, allow('manager'), updateMaintenance);
// r.get('/', validateUser, allow('admin', 'manager'), getMaintenance);

// module.exports = r;
